/*Input two float type variables and add this number. 
Input character for user and display. 
Write program use all function on formatted & unformatted operation. 
An input string and determine value of length. 
Input a string and convert a lower case. 
Input a sting and convert upper case. 
Input two string and comparison its. 
Input two string and concatenate its. 
Program to print numbers form one to one hundred divisible by 7. 
Write a program area of tringle from user input. 
Write a program as your wise. */

#include<stdio.h>

int main(){
    float a,b,c;
    printf("Enter your first float value of a:");
    scanf("%f",&a);
    printf("Enter your Second float value of b:");
    scanf("%f",&b);
    c=a+b;
    printf("Your float type number addition value is :%f",c);

    return 0;
}