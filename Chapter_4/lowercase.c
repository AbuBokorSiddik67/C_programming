#include<stdio.h>
#include<string.h>
int main(){
    char str[50]="Abu BOKOR SIDDIK";
    printf("lower case string is : %s",strlwr(str));
    printf("lower case string is : %s",strupr(str));

    return 0;
}